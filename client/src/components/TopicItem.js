function TopicItem({ }) {
    return (
        // <div key={`topic-${topic.id}`}>
        //     <input 
        //         type="checkbox"
        //         id={`topic-${topic.id}`}
        //         value={topic.topic_name}
        //         onChange={event => handleCheckboxChange(event, topic)}
        //         checked={autoCheckBoxes(topic)}
        //     >
        //     </input>
        //     <label>{topic.topic_name}</label>
        // </div>
        <div></div>
    )
}

export default TopicItem;